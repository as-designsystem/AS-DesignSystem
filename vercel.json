{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @as-designsystem/core build && cd apps/docs && npx vite build",
  "outputDirectory": "apps/docs/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "ignoreCommand": "git diff --quiet HEAD~2 HEAD -- apps/docs/ packages/core/ vercel.json middleware.js api/"
}
